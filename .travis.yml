language: php

sudo: false

php:
  - 7.4
  - 8.1
  - 8.2
  - 8.3

before_script:
  - composer install --prefer-dist --no-interaction

script:
  - vendor/bin/phpunit

cache:
  directories:
    - vendor
    - $COMPOSER_CACHE_DIR
